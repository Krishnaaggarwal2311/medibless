# MedBless Backend Environment Variables
PORT=5000
NODE_ENV=development

# Database (XAMPP default: root + empty password — set DB_PASSWORD if yours is different)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=medbless
DB_USER=root
DB_PASSWORD=

# MongoDB (API uses this; keep DB_* for npm run db:migrate from MySQL)
MONGODB_URI=mongodb+srv://aggarwalkrishna0307_db_user:GifAZ86O4WG82cPI@mediblash.xsrzl5b.mongodb.net/test?retryWrites=true&w=majority
# Local Windows only — fixes TLS cert verification (do NOT set on Render/production)
MONGODB_TLS_INSECURE=true

# JWT
JWT_SECRET=medbless_super_secret_jwt_key_2024
JWT_EXPIRES_IN=7d

# Default dev accounts (optional — used by npm run admin:ensure / patient:ensure)
# PATIENT_EMAIL=krishnaaggarwal2311@gmail.com
# PATIENT_PASSWORD=Krishna@1234

# Email (Nodemailer)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# File Upload
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880

# Frontend URL
CLIENT_URL=http://localhost:3000
# Browser origins allowed (local + deployed admin on Netlify; user site yahan comma se add karo)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,https://clever-begonia-69b4a5.netlify.app